{
  "version": 2,
  "buildCommand": "npm run build-static",
  "builds": [
    { "src": "dist/static/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/static/index.html" }
  ]
}